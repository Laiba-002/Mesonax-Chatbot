"""
Configuration Module - Template
Copy this file to config.py and fill in your actual credentials
"""
import os
from dotenv import load_dotenv

# Load environment variables from .env file
load_dotenv()

# SQL Server Configuration
SQL_CONFIG = {
    'server': os.getenv('SQL_SERVER', '192.168.100.181'),
    'database': os.getenv('SQL_DATABASE', 'Corxnew'),
    'username': os.getenv('SQL_USERNAME', 'sa'),
    'password': os.getenv('SQL_PASSWORD'),
    'driver': os.getenv('SQL_DRIVER', '{ODBC Driver 17 for SQL Server}'),
    'trusted_connection': 'no'
}

# Neo4j Configuration
NEO4J_CONFIG = {
    'uri': os.getenv('NEO4J_URI', 'bolt://localhost:7687'),
    'username': os.getenv('NEO4J_USERNAME', 'neo4j'),
    'password': os.getenv('NEO4J_PASSWORD')
}

# OpenAI Configuration
OPENAI_CONFIG = {
    'api_key': os.getenv('OPENAI_API_KEY'),
    'model': os.getenv('OPENAI_MODEL', 'gpt-4o'),
    'temperature': float(os.getenv('OPENAI_TEMPERATURE', '0.1')),
    'max_tokens': int(os.getenv('OPENAI_MAX_TOKENS', '2000'))
}

# Query Error Handling Configuration
MAX_RETRY_ATTEMPTS = 3

# Batch size for data migration
BATCH_SIZE = 1000

# Logging
LOG_LEVEL = 'INFO'

# ETL Auto-Sync Interval (seconds after first full migration completes)
# 3600 = 1 hour | 1800 = 30 min | 900 = 15 min | 300 = 5 min
DELTA_SYNC_INTERVAL_SECONDS = 3600
